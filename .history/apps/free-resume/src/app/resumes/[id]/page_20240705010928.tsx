import {
  ContainerSeccions,
  Layout,
  StoreProvider,
  TheResume,
} from '@free-resume/resumecomps';
import {
  Box,
  Button,
  ChakraProvider,
  Flex,
  VStack,
  useSteps,
} from '@chakra-ui/react';
import { FormContainer } from 'form-info-curriculum';

export default function Page({ params }: { params: { id: string } }) {
  return <FormContainer />;
}
